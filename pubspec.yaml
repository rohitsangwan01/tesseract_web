name: tesseract_web
description: A Flutter plugin To add tesseract ocr support in WebApp
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  plugin_platform_interface: ^2.0.2
  js: ^0.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      web:
  assets:
    - assets/
